# GENERATED SNAPSHOT -- do not edit by hand and do not load this file at runtime.
#
# The authoritative values live in the dataclass named under `_source` below; this file
# exists so the current configuration can be read and reviewed without opening the code.
# tests/unit/test_config_snapshots.py fails if the two ever disagree, and prints the
# corrected contents when it does.
_source:
  execution_controller: probe_drawer.controllers.execution_pull_controller:ExecutionControllerCfg
execution_controller:
  rise_fraction: 0.1
  fall_fraction: 0.1
  shape: smoothstep
  settle_steps: 30
  zero_force_cleanup_steps: 2
  post_execution_settle_steps: 0
task_parameters_are_not_configuration: peak_force and duration are arguments to ExecutionPullController.run.
  The goal displacement d_goal is deliberately not an input at all (docs/DECISIONS.md
  D004).
reference_operating_point:
  peak_force: 5.0
  duration: 2.0
zero_force_cleanup_note: zero_force_cleanup_steps and post_execution_settle_steps
  run after t = T. They are excluded from the returned history, from duration, from
  d(T)/v(T) and from success evaluation -- see docs/DECISIONS.md D022.
